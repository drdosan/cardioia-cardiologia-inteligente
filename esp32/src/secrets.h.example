/**
 * Copie este arquivo para secrets.h (mesma pasta src/) e preencha com seus valores reais.
 * secrets.h NÃO deve ser commitado (está no .gitignore).
 */
#pragma once

#define WIFI_SSID "SUA_REDE_WIFI"
#define WIFI_PASS "SUA_SENHA_WIFI"

/** HiveMQ Cloud: cluster → host, porta TLS 8883, credenciais da aba Access Management */
#define MQTT_HOST "xxxx.s1.eu.hivemq.cloud"
#define MQTT_PORT 8883
#define MQTT_USER "seu_usuario_mqtt"
#define MQTT_PASS "sua_senha_mqtt"

/** Tópico de telemetria (ver docs/fase3-arquitetura.md) */
#define MQTT_TOPIC_TELEMETRY "cardioia/f3/esp32-wokwi-01/telemetry"

/** 1 = também envia cada JSON pelo Serial (útil no laboratório); 0 = só MQTT */
#define CARDIOIA_SERIAL_MIRROR 0
